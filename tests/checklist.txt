уже реализовано

основное:

память - стек + куча
основные операции
переменные
строки
массивы
условные операторы
циклы
функции

мелочи:

вызов функций из самих себя
параметры по умолчанию
пропуск второго выражение в or, and
срезы строк и массивов
метод для длинны строки или массива
push pop для массива
split, join
codeOfChar выводит код символа на i-й позиции в строке
print
input
format - переводит объект в строку для "красивого" вывода
clone - гдубокая копия объекта
is_type
to_type
continue, break
 